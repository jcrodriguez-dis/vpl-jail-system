bin_PROGRAMS = vpl-xmlrpc-jail
vpl_xmlrpc_jail_SOURCES = vpl-xmlrpc-jail.cpp jail.cpp httpServer.cpp redirector.cpp jail.h httpServer.h  util.h rpc.h xml.h redirector.h

# set the include path found by configure
#INCLUDES= $(all_includes)

# the library search path.
vpl_xmlrpc_jail_LDFLAGS = $(all_libraries) -lutil
